import images from "../../../assets/images";

function MobiePage() {
  return (
    <div
      className="bg-no-repeat bg-cover bg-center w-full h-full"
      style={{ background: `url(${images.mobi_bg})` }}
    ></div>
  );
}

export default MobiePage;
